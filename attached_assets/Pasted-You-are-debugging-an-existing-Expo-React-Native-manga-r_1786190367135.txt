You are debugging an existing Expo/React Native manga reader application.

IMPORTANT:
Do NOT redesign the application.
Do NOT rebuild the translation system.
Do NOT modify unrelated features.
Do NOT create new architecture unless the existing implementation is proven to be the cause.
Do NOT spend time exploring the whole project unnecessarily.
Work with the minimum number of file reads and edits possible.

CURRENT BUG:

When the user opens a manga page and presses "Translate", the application shows:

"Page 4 Translation Failed"

"Unable to reach the translation servers. Check your internet connection and try again."

Technical detail:

"Failed to fetch"

The device has a working internet connection.

The application uses Gemini API keys entered by the user locally in the app.

The application is designed to work without our own translation backend/server.

GOAL:

Find the EXACT reason why the translation request fails and fix only that problem.

==================================================
PHASE 1 — TRACE THE EXISTING TRANSLATION FLOW

First inspect only the files directly responsible for translation.

Trace this exact flow:

Translate button
→ translation function
→ API key selection
→ selected Gemini model
→ Gemini request construction
→ fetch/request execution
→ HTTP response
→ JSON parsing
→ translation extraction
→ renderer

Identify the exact function where "Failed to fetch" originates.

Do NOT assume that the problem is the internet.

Check whether the error is generated by:

- fetch()
- AbortController timeout
- invalid URL
- invalid API endpoint
- invalid API key
- incorrect model name
- malformed request body
- unsupported Gemini API format
- incorrect headers
- network security configuration
- response parsing
- API quota/rate limit
- model availability
- empty response
- incorrect error handling

==================================================
PHASE 2 — VERIFY GEMINI CONFIGURATION

Inspect the existing Gemini implementation.

Verify:

1. API endpoint
2. HTTP method
3. Content-Type
4. Authorization/API-key mechanism
5. model name
6. request body structure
7. generation configuration
8. response parsing
9. timeout
10. retry logic

The application must use the Gemini API directly from the Expo/React Native client if that is how the current architecture was designed.

Do NOT introduce a new backend/server just to hide the problem.

Do NOT move API keys to a server.

Do NOT replace the existing API architecture unless absolutely required.

==================================================
PHASE 3 — VERIFY THE USER API KEY SYSTEM

The application supports multiple Gemini API keys.

Verify that the translation request actually receives the currently active key.

Check:

- key retrieval
- active key selection
- key masking/display
- empty key handling
- malformed key handling
- key rotation
- exhausted-key handling
- model compatibility

IMPORTANT:

Never expose the complete API key in logs, UI, or error messages.

Only log safe information such as:

key index
model name
request status
HTTP status
error category

==================================================
PHASE 4 — VERIFY MODEL SELECTION

The application allows the user to select a Gemini model.

Do not assume the model shown in the UI is actually the model used by the request.

Verify that:

UI selected model

stored model

model passed to Gemini API

If the selected model is unavailable or invalid, return a clear error instead of the generic:

"Failed to fetch"

Also make sure the application does not accidentally use an obsolete or incorrect model identifier.

==================================================
PHASE 5 — ADD TEMPORARY DIAGNOSTIC LOGGING

Add minimal diagnostic logging around ONLY the translation request.

Log:

[TRANSLATION START]
page number
model
key index
request URL hostname/path WITHOUT API key
request start time

Then log:

[TRANSLATION RESPONSE]
HTTP status
elapsed time
response content type

Or:

[TRANSLATION NETWORK ERROR]
error name
error message
elapsed time

Or:

[TRANSLATION API ERROR]
HTTP status
safe error category
safe response message

NEVER log the API key.

NEVER log the complete request body if it may contain sensitive data.

Keep the diagnostics minimal and targeted.

==================================================
PHASE 6 — TEST THE ACTUAL REQUEST

Do not merely inspect the code.

Run the smallest possible real translation test using one manga page.

Test:

1. active API key
2. selected Gemini model
3. one page
4. one translation request

Determine whether the failure occurs before the request reaches Gemini or after Gemini responds.

If the request reaches Gemini and returns an HTTP error, fix the actual API error.

If fetch itself fails, determine whether the URL, network request, timeout, or React Native environment is responsible.

==================================================
PHASE 7 — FIX THE ROOT CAUSE ONLY

After identifying the exact cause, implement the smallest safe fix.

Examples:

If endpoint is wrong:
→ correct the endpoint.

If model identifier is wrong:
→ correct the model mapping.

If request body is wrong:
→ correct only the request structure.

If response parsing is wrong:
→ correct the parser.

If timeout is too aggressive:
→ adjust it reasonably.

If retry logic incorrectly treats normal API errors as network failures:
→ correct the error classification.

If API quota is exhausted:
→ make the application rotate to the next available user key.

If a key is invalid:
→ mark that key unavailable and move to the next valid key.

If the API returns a rate-limit response:
→ respect the retry information when available and use the existing key rotation system.

Do NOT create unnecessary abstractions.

==================================================
PHASE 8 — IMPROVE ERROR MESSAGES

The current message:

"Unable to reach the translation servers."

is misleading because this application does not necessarily use a translation server.

Replace generic errors with useful categories:

- No internet connection
- Gemini API key invalid
- Gemini API quota/rate limit reached
- Selected model unavailable
- Gemini API request rejected
- Request timed out
- Gemini returned an empty response
- Unexpected API response
- Unknown network error

The user should know what actually happened.

Technical details should remain hidden behind an expandable "Technical details" section.

==================================================
PHASE 9 — PRESERVE EXISTING FEATURES

Do not break:

- manga sources
- chapter loading
- image caching
- reader
- image preloading
- OCR
- image cleaning/inpainting
- API key management
- model selection
- settings
- source adapters
- library
- favorites
- reading history

Do not modify files unrelated to the translation bug.

==================================================
PHASE 10 — FINAL VALIDATION

After the fix, test exactly:

TEST 1:
Open one manga chapter.

TEST 2:
Translate one page.

TEST 3:
Translate multiple pages.

TEST 4:
Use the currently active Gemini key.

TEST 5:
Verify that the selected model is actually used.

TEST 6:
If multiple keys exist, verify key rotation when the active key cannot be used.

TEST 7:
Verify that successful translations are rendered correctly.

TEST 8:
Verify that an API failure produces a meaningful error instead of "Failed to fetch".

TEST 9:
Verify that no API key is exposed in logs or UI.

IMPORTANT:

Do not claim the problem is fixed until an actual translation request succeeds.

==================================================
RESOURCE CONSTRAINT

This project is being developed on Replit's limited free plan.

Therefore:

- minimize file exploration
- do not read the entire repository
- inspect only translation-related files first
- avoid unnecessary refactoring
- avoid rewriting existing working code
- make the smallest possible patch
- do not create unnecessary files
- do not install new dependencies unless absolutely necessary

Before making changes, identify the smallest set of files that must be modified.

At the end provide a concise report:

1. Root cause
2. Files inspected
3. Files modified
4. Exact fix
5. Translation test result
6. Whether Gemini responded successfully
7. Any remaining limitation

MOST IMPORTANT:

Do not stop at "Failed to fetch".

Find out exactly WHY the fetch fails and fix that specific cause.